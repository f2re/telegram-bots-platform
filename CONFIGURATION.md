# 🔧 Конфигурация платформы

## Быстрый старт

### 1. Запустить мастер настройки

```
./config-wizard.sh
```

Интерактивный мастер задаст все необходимые вопросы и создаст файл `config.env`.

### 2. Или скопировать шаблон вручную

```
cp config.env.example config.env
nano config.env
```

### 3. Запустить установку

```
sudo ./setup-server.sh
```

## Основные параметры

### SSH
- `SSH_PORT` - Порт SSH (рекомендуется изменить с 22)
- `SSH_PASSWORD_AUTH` - Разрешить вход по паролю (рекомендуется "no")

### PostgreSQL
- `POSTGRES_MAX_CONNECTIONS` - Максимум подключений
- `POSTGRES_SHARED_BUFFERS` - Размер буфера (25% RAM)
- `POSTGRES_EFFECTIVE_CACHE_SIZE` - Размер кэша (50-75% RAM)

### Nginx
- `NGINX_CLIENT_MAX_BODY_SIZE` - Максимальный размер загружаемого файла
- `NGINX_RATE_LIMIT_API` - Ограничение запросов к API

### Мониторинг
- `MONITORING_ENABLED` - Включить Grafana + Prometheus
- `GRAFANA_PORT` - Порт Grafana (по умолчанию 3000)

## Автогенерация паролей

Следующие пароли генерируются автоматически, если оставить пустыми:
- `POSTGRES_PASSWORD`
- `GRAFANA_ADMIN_PASSWORD`

Они сохраняются в `/root/.platform/` после установки.

## Валидация конфигурации

Конфигурация автоматически проверяется при запуске скриптов.

## Изменение конфигурации после установки

```
nano config.env
sudo ./scripts/apply-config-changes.sh
```